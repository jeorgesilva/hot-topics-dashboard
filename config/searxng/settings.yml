general:
  instance_name: "hot-topics-searxng"
  debug: false

server:
  secret_key: "hot-topics-searxng-local-dev-key-change-in-prod"
  bind_address: "0.0.0.0"
  port: 8080

search:
  formats:
    - html
    - json

use_default_settings: true
